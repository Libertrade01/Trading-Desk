import SidebarIconOptions from "./SidebarIconOptions";

export const metadata = {
  title: "Sidebar icon options | LOOP",
  description: "Alternative icon directions for the LOOP sidebar.",
};

export default function SidebarIconOptionsPage() {
  return <SidebarIconOptions />;
}
